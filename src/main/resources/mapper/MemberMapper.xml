<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lxpantos.auth.adapter.out.persistence.mybatis.MemberMapper">

    <resultMap id="memberResultMap"
               type="com.lxpantos.auth.adapter.out.persistence.mybatis.MemberPersistenceModel">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="createdAt" column="created_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM members
        WHERE email = #{email}
    </select>

    <select id="findByEmail" resultMap="memberResultMap">
        SELECT id, email, password_hash, name, phone_number, created_at, last_login_at
        FROM members
        WHERE email = #{email}
        LIMIT 1
    </select>

    <insert id="insert"
            parameterType="com.lxpantos.auth.adapter.out.persistence.mybatis.MemberPersistenceModel"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO members (email, password_hash, name, phone_number, created_at)
        VALUES (#{email}, #{passwordHash}, #{name}, #{phoneNumber}, #{createdAt})
    </insert>

    <update id="updateLastLoginAt">
        UPDATE members
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{memberId}
    </update>
</mapper>
