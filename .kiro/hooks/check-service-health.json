{
  "version": "v1",
  "hooks": [
    {
      "name": "지금 기능이 정상인지 확인하기",
      "description": "비개발자(기획·현업·SM 담당자)가 버튼처럼 눌러 직접 실행하는 hook. 기존 화면과 기능이 예전 그대로 동작하는지 확인하고, 그 결과를 개발 용어 없이 쉬운 말로 로그(.kiro/hooks/logs/check-service-health.log)에 남긴다. 내부적으로 기존 기능 동작 확인(./mvnw test)을 실행한다.",
      "trigger": "Manual",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .kiro/hooks/check-service-health.ps1"
      },
      "enabled": true
    }
  ]
}
